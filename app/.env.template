# Dapanoskop — Local Development Environment Variables
#
# Copy this file to .env and adjust values as needed.
# .env files are gitignored and will not be committed.
#
#   cp .env.template .env

# ─── Auth Bypass ───────────────────────────────────────────────────────
# Set to "true" to skip Cognito authentication during local development.
# When enabled, the app assumes the user is always authenticated and
# reads data from the /data/ endpoint (local fixtures or S3 proxy).
VITE_AUTH_BYPASS=true

# ─── Data Source ───────────────────────────────────────────────────────
# By default, the dev server serves fixture data from the fixtures/ directory.
# To use real data from S3, set VITE_DATA_S3_BUCKET to your bucket name.
# The Vite dev server will proxy /data/* requests to S3, using your local
# AWS credentials (SSO profile, environment variables, ~/.aws/credentials).

# VITE_DATA_S3_BUCKET=dapanoskop-data-abc123
# VITE_DATA_S3_REGION=eu-north-1

# ─── Production Config (not needed for local dev) ─────────────────────
# These are normally injected by Terraform via config.json in production.
# Only set these if you need to test the full Cognito auth flow locally.

# VITE_COGNITO_DOMAIN=https://dapanoskop.auth.eu-north-1.amazoncognito.com
# VITE_COGNITO_CLIENT_ID=
# VITE_USER_POOL_ID=eu-north-1_XXXXXXXXX
# VITE_IDENTITY_POOL_ID=eu-north-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# VITE_AWS_REGION=eu-north-1
# VITE_DATA_BUCKET_NAME=dapanoskop-data-abc123
# VITE_REDIRECT_URI=http://localhost:5173/
